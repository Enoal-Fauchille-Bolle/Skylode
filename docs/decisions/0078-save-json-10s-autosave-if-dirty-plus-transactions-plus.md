# 0078 — Save: JSON, 10s autosave (if dirty) plus transactions plus exit, atomic…

**Status:** accepted
**Tags:** save
**Supersedes:** —
**Superseded by:** —

## Decision

Save: JSON, 10s autosave (if dirty) plus transactions plus exit, atomic write, `.bak`,
HMAC, versioning

## Why

See [SYSTEMS.md](../SYSTEMS.md#save-system).
